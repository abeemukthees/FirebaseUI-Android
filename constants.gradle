project.ext {
    submodules = ['database', 'auth', 'storage']
    group = 'com.firebaseui'
    version = '2.0.2'
    pomdesc = 'Firebase UI Android'

    compileSdk = 25
    targetSdk = 25
    minSdk = 14

    buildTools = '26.0.0' // Remember to also update in .travis.yml

    firebaseVersion = '11.0.2'
    supportLibraryVersion = '25.4.0'
}
